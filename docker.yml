name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.whatrulookinat24 }}
          password: ${{ secrets.1AMme2012! }}

      - name: Build Docker Image
        run: docker build -t whatrulookinat24/whattest:what

      - name: Push Docker Image
        run: docker push us whatrulookinat24/whattest:what
